# ============================================================
# Tolstoy AI Studio — Requirements
# Python: >=3.9 (рекомендуется 3.10–3.12)
# ============================================================

# ---------- Ядро ----------
# PyTorch >= 2.2.0 критичен для:
#   • native GQA (enable_gqa в scaled_dot_product_attention)
#   • torch.compile (regional compilation)
#   • fused AdamW
#   • bfloat16 автокаст на CUDA/XPU
torch>=2.2.0

# ---------- CLI & Логирование ----------
rich>=13.0.0          # Интерактивный интерфейс, таблицы, прогресс-бары, цветной вывод

# ---------- Конфигурации ----------
PyYAML>=6.0           # Чтение/запись YAML-пресетов (config/__init__.py)

# ---------- Системный мониторинг ----------
psutil>=5.9.0         # Определение RAM, CPU, авто-выбор конфигурации под железо

# ---------- Безопасность ----------
defusedxml            # Защита от XXE-атак при парсинге XML

# ---------- Математика (зависимость torch, но фиксируем совместимость) ----------
numpy>=1.24.0

# ============================================================
# ОПЦИОНАЛЬНЫЕ (без них проект запустится, но часть функций отключится)
# ============================================================

# Загрузка русскоязычных датасетов (Lenta, Taiga, Wiki и др.)
# Если не установлен — пункт меню «Архив датасета через Corus» будет недоступен
corus>=0.10.0

# Мониторинг VRAM на NVIDIA GPU
# Если не установлен — статистика GPU будет недоступна, обучение всё равно работает
pynvml>=11.0.0